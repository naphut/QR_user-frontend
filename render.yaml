{
  "version": 1,
  "name": "qr-user-frontend",
  "type": "static",
  "build": {
    "commands": ["npm run build"]
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
